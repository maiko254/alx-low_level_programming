#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @j: integer to be computed
 *
 * Return:0 Always zero
 */
int _abs(int j)
{
	if (j >= 0)
		return (j);
	else
		return (-j);
}
