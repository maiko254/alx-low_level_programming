#include <stdio.h>

/**
 * main function - prints exactly "Programming is like building is like building a multilingual puzzle
 * Return 0 if success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
